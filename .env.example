# Copy to .env.local and fill in as needed.
#
# NEXT_PUBLIC_E2E=1 — exposes window.__editor so Playwright can drive tldraw.
# Leave unset for local dev unless you're running the E2E suite; CI sets it at
# the job level so the prod bundle Playwright serves carries the hook.
